apiVersion: apps/v1
kind: Deployment
metadata:
  name: holdwall-worker
  namespace: holdwall
  labels:
    app: holdwall
    component: worker
    managed-by: kustomize
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: holdwall
      component: worker
      managed-by: kustomize
  template:
    metadata:
      labels:
        app: holdwall
        component: worker
        managed-by: kustomize
    spec:
      serviceAccountName: holdwall-worker
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        fsGroup: 1001
      containers:
        - name: pipeline-worker
          image: 597743362576.dkr.ecr.us-east-1.amazonaws.com/holdwall-pos:20260124-221959
          workingDir: /app
          command:
            [
              "sh",
              "-lc",
              "npx tsx -e \"require('./lib/workers/pipeline-worker').startPipelineWorker().catch((e)=>{ console.error(e); process.exit(1); })\"",
            ]
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: holdwall-secrets
                  key: DATABASE_URL
            - name: REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: holdwall-secrets
                  key: REDIS_URL
            - name: KAFKA_BROKERS
              valueFrom:
                secretKeyRef:
                  name: holdwall-secrets
                  key: KAFKA_BROKERS
          envFrom:
            - configMapRef:
                name: holdwall-config
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "2Gi"
              cpu: "2000m"
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            capabilities:
              drop:
                - ALL
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - holdwall
                    - key: component
                      operator: In
                      values:
                        - worker
                topologyKey: kubernetes.io/hostname
